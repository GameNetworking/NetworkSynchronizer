#pragma once

// TODO convert to DOC-TEST
// I din't that right away because at the moment there are some modules that
// doesn't support it and it's difficult to test it.

void test_scene_synchronizer();
